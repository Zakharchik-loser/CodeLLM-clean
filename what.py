#
# numbers=[int(i) for i in input().split()]
# suma=0
#
# for number in numbers:
#     suma+=number
#
# print(suma)
#

colors = ['red', 'green', 'blue', 'yellow']

for i in colors:
    print(i)

    # def node_or_something(state: State):
    #     return {"messages":[llm.invoke(state["messages"])]}
    #
    # graph_builder.add_node("node_or_something",node_or_something)
    #
    # graph_builder.add_edge(START,"node_or_something")
    #
    # graph_builder.add_edge("node_or_something",END)
    #
    # graph = graph_builder.compile()
    #
    # input = input("Enter Something")
    # state = graph.invoke({"messages":[{"role":"user","content": input}]})
    #
    # print(state["messages"][-1].content)

